# fault

Error handling for Go: stack-traced errors, tagged errors (user fault, auth,
retryable), cause coalescing, and panic recovery. Built on `github.com/pkg/errors`.

```sh
go get github.com/gleno/fault
```
